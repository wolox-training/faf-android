<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="ar.com.wolox.android">

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />

    <application
        android:name=".example.BootstrapApplication"
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:theme="@style/AppTheme">
        <activity android:name=".example.ui.RootActivity"
            android:screenOrientation="portrait"
            android:windowSoftInputMode="adjustResize"
            android:noHistory="true"
            >
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name=".example.ui.login.LoginActivity"
            android:screenOrientation="portrait"
            android:windowSoftInputMode="adjustResize"
            android:noHistory="true"
           >
        </activity>
        <activity android:name=".example.ui.example.ExampleActivity" />
        <activity android:name=".example.ui.signup.SignUpActivity" />
        <activity android:name=".example.ui.hompage.HomePageActivity" />
        <activity android:name=".example.ui.viewpager.homepageviewpager.HomePageViewPagerActivity" />
        <activity
            android:name=".example.ui.viewpager.ViewPagerActivity"
            android:screenOrientation="portrait" />
    </application>

</manifest>